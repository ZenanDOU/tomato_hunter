## 1. DailyPlanBoard.tsx

- [x] 1.1 将 `handleStartHunt` 的 `useCallback`（第 69 行）移到 early return（第 55 行）之前

## 2. HuntApp.tsx

- [x] 2.1 将 `handleStartNextHunt` 的 `useCallback`（第 108 行）移到 early return（第 104 行）之前

## 3. 验证

- [x] 3.1 确认 `npm run build` 编译通过，无 hooks 相关警告
